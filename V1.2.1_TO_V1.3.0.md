# Auto Playlist Maker 1.2.1 → 1.3.0 변경 사항

작성 기준: `D:\aldente yt\AutoPlaylistMaker_v1.2.1`과
`D:\aldente yt\AutoPlaylistMaker_v1.3.0`의 실제 소스 비교

## 요약

1.3.0은 1.2.1의 단일 대형 편집·렌더 화면을 디자인/효과 단계와
렌더 단계로 분리하고, 다국어·프로젝트 상태·미리보기·반복 렌더링을
공통 상태와 공통 계산 함수 중심으로 정리한 버전이다.

| 항목 | 1.2.1 | 1.3.0 |
|---|---|---|
| 디자인/렌더 구조 | `Stage3VideoEdit` 한 클래스 | `Stage4DesignEffects`와 `Stage5Render`로 분리 |
| 앱 본체 `app.py` | 약 4,938줄 | 약 4,112줄 |
| 다국어 | 부분 적용 | 9개 언어 JSON과 무결성 검사 |
| 반복 설정 | 렌더 화면 편집 | 디자인/효과 화면에서 편집, 렌더 화면은 요약 |
| 프로젝트 상태 | 일부 페이지 상태 | 디자인·비주얼라이저·반복·출력·가시성 상태 저장 |
| 배포 | 기존 단일 실행 파일 중심 | 1.3.0 전용 폴더형 배포와 ZIP |
| 자동 테스트 | 루트의 5개 테스트 파일 | `tests/`의 7개 테스트 파일, 48개 테스트 |

`requirements.txt`의 10개 직접 의존성 범위는 동일하다.

## 사용자 기능 변경

### 음원 편집

- DAW 형태의 파형 타임라인과 트림 핸들을 제공한다.
- 핸들을 누른 상태에서만 트림 값이 변경된다.
- 포인터 캡처 해제, 영역 밖 드래그 종료, 최소 트림 길이, 스냅과
  Shift/Alt 미세 조절을 지원한다.
- 음량, 페이드 인·아웃과 트림 상태를 프로젝트에 저장한다.

### 수동 분배

- 곡 사이 삽입 영역과 삽입선을 표시한다.
- 그룹 내부·그룹 간·빈 그룹 이동을 처리한다.
- 그룹이 하나도 없을 때 첫 이동으로 `Mix 1`을 자동 생성한다.
- 버튼 이동과 드래그 이동이 같은 이동 로직을 사용한다.

### 클립

- 이미지와 영상의 대표 프레임 미리보기를 제공한다.
- 썸네일은 UI 스레드를 막지 않고 생성하며 경로와 수정시간을 기준으로
  캐시한다.

### 디자인과 미리보기

- 검색형 공통 폰트 선택기를 사용한다.
- 출력 해상도 기준 레이아웃을 축소해 미리보기에 표시한다.
- 2곡 미리보기의 30초 제한을 제거했다.
- 이미지, 로고, 텍스트, 비주얼라이저의 위치와 크기 설정 범위를
  확대했다.
- 디자인 미리보기와 최종 출력의 해상도·FPS 값이 양방향으로 동기화된다.

### 비주얼라이저

- 막대형, 미니멀 라인형, 스펙트럼형, 원형, 중앙 방사형을 제공한다.
- 개수, 폭, 간격, 최소·최대 높이, 감도, 스무딩, 감쇠, 대칭, 반전,
  그라데이션, 글로우, 선 굵기와 X/Y/너비를 설정할 수 있다.
- 모든 스타일이 X/Y 위치 설정을 실제 렌더링에서 사용한다.

### 완성 영상 반복

- 반복 횟수와 목표 재생시간 방식을 제공한다.
- 목표시간 방식은 `ceil(목표시간 / 1회 길이)`로 계산하며 마지막
  반복을 자르지 않는다.
- 반복 설정 편집 위치를 디자인/효과 단계로 통합했다.
- 렌더 단계에는 같은 설정의 읽기 전용 요약과 이동 버튼만 남겼다.
- 빈 숫자 입력은 편집 중 허용하고 Enter 또는 포커스 이동 시 검증한다.
- 분·초가 60 이상이면 정상 H:M:S로 정규화한다.
- 대기열과 완료 상태의 예상 길이도 최종 반복 길이를 사용한다.

### 화면 표시 시간

- 기존 120초 슬라이더 대신 시간·분·초 입력을 사용한다.
- 시작 후 화면을 끄고, 선택적으로 최종 종료 전에 다시 켤 수 있다.
- 검은 구간에도 오디오는 계속 재생된다.
- 반복 경계마다 화면이 다시 켜지지 않고 최종 출력 전체 타임라인을
  기준으로 한 번만 계산한다.

### 저장과 호환성

- 프로젝트 버전과 앱 상태 브랜치를 마이그레이션한다.
- 이전 반복 설정과 화면 표시 설정을 새 형식으로 복원한다.
- 누락 미디어를 유지하고 폴더 이동 후 재연결할 수 있다.
- 자동 저장과 수동 저장이 같은 직렬화 경로를 사용한다.

### 다국어와 한국어 UI

- 한국어, 영어, 일본어, 중국어 간체·번체, 스페인어, 프랑스어,
  이탈리아어, 아랍어를 지원한다.
- 언어 변경 후 내부 선택값은 번역 문구가 아닌 안정적인 ID를 유지한다.
- 한국어의 `켈기`, `까빡임`, `캄롯`, `반복 횟와` 등의 오타를
  `켜기`, `깜빡임`, `카멜롯`, `반복 횟수와`로 수정했다.
- 번역 키 누락, 중복 키, placeholder 불일치, 깨진 문자열을 검사하는
  `check_locales.py`를 추가했다.

## 내부 구조 변경

새로 분리되거나 추가된 주요 모듈:

- `stage4_design_effects.py`: 디자인, 효과, 미리보기, 반복 설정
- `stage5_render.py`: 출력 옵션, 대기열, 렌더 작업
- `i18n.py`, `locales/`: 다국어 리소스
- `ffmpeg_service.py`: FFmpeg 검색과 MoviePy 연결
- `timeline_utils.py`: 트랙 경계, 2곡 구간, 최종 가시성 계산
- `check_locales.py`: 번역 리소스 정적 검사

렌더링은 긴 오디오를 Python 메모리에 모두 올리지 않는 FFmpeg 스트리밍
파이프라인을 사용한다. FFmpeg 8에서 입력측 `-ss/-t`, `afade`,
`acrossfade` 조합이 출력을 비정상적으로 단축하는 문제는
`atrim + asetpts` 필터 그래프로 변경해 해결했다.

## 전체 코드 리뷰에서 추가로 발견해 수정한 문제

- 프로젝트 저장 실패 알림이 Tk 예약 콜백에서 삭제된 예외 변수 `e`를
  참조해 실제 오류 대신 `NameError`를 낼 수 있던 문제를 수정했다.
- 전체 렌더 실패 알림에도 같은 예외 변수 수명 문제가 있어 오류 문자열을
  콜백 기본 인자로 안전하게 보존하도록 수정했다.
- 렌더 대기열이 렌더 시작 후 다시 기본 플레이리스트 길이로 표시되던
  문제를 최종 반복 길이 기준으로 통일했다.
- 디자인 미리보기와 렌더 단계의 해상도·FPS가 별도 변수라 서로 다를 수
  있던 문제를 양방향 동기화했다.
- 효과 단계와 렌더 단계가 서로 다른 반복 설정 객체를 사용하던 구조를
  하나의 상태 객체로 통합했다.

## 검증

- Python 구문 검사 통과
- 직접 의존성 검사 `pip check` 통과
- 9개 언어 리소스 무결성 검사 통과
- 전체 자동 테스트 48개 통과
- 실제 FFmpeg 파일 반복 테스트에서 전체 복제와 출력 길이 확인
- 폴더형 EXE 실행, 메인 창 표시, 정상 종료와 잔류 프로세스 확인

## 알려진 제한

- 첫 패키지 실행은 대형 과학·오디오 라이브러리 로딩 때문에 느리다.
- 영상 썸네일은 현재 약 1초 지점을 사용하며 검은 도입부 자동 회피는
  아직 지원하지 않는다.
- 빌드 경고의 `tbb12.dll`은 Numba의 선택적 TBB 백엔드에 관한 것이며
  현재 앱의 기본 분석·렌더 경로는 해당 백엔드를 사용하지 않는다.
- 실제 수 시간 분량의 장시간 렌더는 자동 테스트 비용상 매 빌드마다
  수행하지 않으며, 짧은 실파일로 동일 파이프라인을 검증한다.
